name: Orchestration — build & push image to GHCR

# Scoped to Orchestration/ and its ReAct dependency.

on:
  push:
    branches: [master]
    paths:
      - "Orchestration/**"
      - "ReAct/**"
      - "mcp_client_app/mcp_client.py"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build:
    name: Orchestration → GHCR
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      # GHCR paths must be lowercase; github.repository_owner may be mixed case (e.g. GangadharanS).
      - name: Set GHCR owner (lowercase)
        run: echo "GHCR_OWNER=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.GHCR_OWNER }}/pr-orchestrator
          tags: |
            type=raw,value=latest
            type=sha,prefix=sha-
            type=ref,event=branch

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Orchestration/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=pr-orchestrator
          cache-to: type=gha,scope=pr-orchestrator,mode=max

  deploy:
    needs: build
    if: github.ref == 'refs/heads/master'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/orchestration-deploy-azure.yml
    secrets: inherit
