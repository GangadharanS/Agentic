# A2A HTTP agent mesh
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# LLM + MCP compatibility with ReAct/
google-generativeai>=0.8.0
python-dotenv>=1.0.0
aiohttp>=3.9.3
pydantic>=2.9.0

# Reuses ReAct/ via sys.path injection — no install required if ReAct/ is a sibling folder.
