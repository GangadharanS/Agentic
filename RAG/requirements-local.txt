# Optional dependencies, used only when EMBEDDING_PROVIDER=local.
# These are installed lazily on first use by app/embedding_provider.py.
# You can also pre-install them inside the image by adding the following line
# to the Dockerfile, which makes the first ingest/query fast at the cost of
# ~600 MB extra image size:
#
#   RUN pip install --no-cache-dir torch==2.4.1+cpu \
#       --index-url https://download.pytorch.org/whl/cpu
#   RUN pip install --no-cache-dir -r requirements-local.txt

sentence-transformers==3.1.1
