fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.9
pydantic==2.9.2

# Vector DB
chromadb==0.5.7
pinecone-client==5.0.1

# Document loading
langchain==0.3.1
langchain-community==0.3.1
pypdf==4.3.1
unstructured==0.15.13

# HTTP client (Ollama / Gemini / Groq APIs)
httpx==0.27.2

# NOTE: torch + sentence-transformers are NOT installed here.
# They are lazily installed at runtime only when EMBEDDING_PROVIDER=local.
# See requirements-local.txt for the pinned versions used by that lazy install.
